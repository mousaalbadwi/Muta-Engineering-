@model IEnumerable<MutaEngineering.Models.AcademicAlert>
@using System.Globalization

@{
    var isRtl = CultureInfo.CurrentUICulture.TextInfo.IsRightToLeft;
    ViewData["Title"] = isRtl ? "الرئيسية" : "Home";
}

@section Styles {
    <link rel="stylesheet" href="~/css/home-premium.css" asp-append-version="true" />
}

<!-- ================= HERO SECTION (Full Screen) ================= -->
<section class="modern-hero">
    <div class="hero-video-bg">
        <video autoplay muted loop playsinline style="width:100%; height:100%; object-fit:cover;">
            <source src="~/videos/engineering-hero.mp4" type="video/mp4" />
        </video>
        <div class="hero-overlay-gradient"></div>
    </div>

    <div class="hero-content-wrapper">
        <span class="hero-badge" style="background: rgba(255,255,255,0.1); padding: 5px 15px; border-radius: 20px; text-transform:uppercase; letter-spacing:2px; font-size:0.8rem; border:1px solid rgba(255,255,255,0.2);">
            @(isRtl ? "جامعة مؤتة" : "Mutah University")
        </span>
        
        <h1 class="hero-title-main">
            @(isRtl
                ? "كلية الهندسة – تعليم هندسي بمعايير عالمية"
                : "Faculty of Engineering – Global Standards in Engineering Education")
        </h1>

        <p class="hero-subtitle">
            @(isRtl
                ? "برامج أكاديمية معتمدة، كوادر متميزة، وبيئة تعليمية حديثة لإعداد مهندسي المستقبل."
                : "Accredited programs, distinguished faculty, and a modern learning environment.")
        </p>

        <div class="hero-actions">
            <a asp-controller="Departments" asp-action="Index" class="btn-modern btn-modern-primary">
                @(isRtl ? "الأقسام الأكاديمية" : "Academic Departments")
            </a>
            <a asp-controller="Admissions" asp-action="Index" class="btn-modern btn-modern-glass">
                @(isRtl ? "القبول" : "Admissions")
            </a>
        </div>
    </div>
    
    <svg class="hero-wave-bottom" viewBox="0 0 1440 120" preserveAspectRatio="none">
        <path d="M0,60 C240,120 480,0 720,20 960,40 1200,100 1440,60 L1440,120 L0,120 Z" />
    </svg>
</section>

<!-- ================= FLOATING STATS ================= -->
<div class="container stats-floating-bar">
    <div class="glass-stats">
        <div class="stat-item-modern">
            <span class="stat-number-modern counter" data-target="7">0</span>
            <span class="stat-label-modern">@(isRtl ? "أقسام هندسية" : "Departments")</span>
        </div>
        <div class="stat-item-modern">
             <span class="stat-number-modern counter" data-target="120">+0</span>
            <span class="stat-label-modern">@(isRtl ? "أعضاء هيئة تدريس" : "Faculty")</span>
        </div>
        <div class="stat-item-modern">
             <span class="stat-number-modern counter" data-target="8000">+0</span>
            <span class="stat-label-modern">@(isRtl ? "طالب" : "Students")</span>
        </div>
        <div class="stat-item-modern">
             <span class="stat-number-modern counter" data-target="1996">0</span>
            <span class="stat-label-modern">@(isRtl ? "سنة التأسيس" : "Established")</span>
        </div>
    </div>
</div>

<!-- ================= SERVICES (BENTO GRID) ================= -->
<section class="modern-section" id="services">
    <div class="container">
        <div class="text-center mb-5">
            <span class="section-label">Services Overview</span>
            <h2 class="section-heading-big">@(isRtl ? "الخدمات الأكاديمية" : "Academic Services")</h2>
            <p class="text-muted" style="max-width: 600px; margin: 0 auto;">
                Learn more about the Services, and academic excellence of the Faculty of Engineering at Mutah University.
            </p>
        </div>

        <div class="bento-grid">
            <!-- Departments (Span 2) -->
            <a asp-controller="Departments" asp-action="Index" class="bento-card bento-span-2 accent-card fade-up">
                <i class="bi bi-building bento-icon"></i>
                <h3 class="bento-title">@(isRtl ? "الأقسام الأكاديمية" : "Departments")</h3>
                <p class="bento-text">@(isRtl ? "التخصصات الهندسية المعتمدة" : "Accredited engineering disciplines")</p>
            </a>

            <!-- Study Plans -->
            <a asp-controller="StudyPlans" asp-action="Index" class="bento-card fade-up delay-1">
                <i class="bi bi-journal-text bento-icon"></i>
                <h3 class="bento-title">@(isRtl ? "الخطط الدراسية" : "Study Plans")</h3>
                <p class="bento-text">@(isRtl ? "الخطط المعتمدة لجميع التخصصات" : "Approved academic study plans")</p>
            </a>

            <!-- Faculty -->
            <a asp-controller="Faculty" asp-action="Index" class="bento-card fade-up delay-2">
                <i class="bi bi-people bento-icon"></i>
                <h3 class="bento-title">@(isRtl ? "الهيئة التدريسية" : "Faculty")</h3>
                <p class="bento-text">@(isRtl ? "أعضاء هيئة تدريس مؤهلين" : "Highly qualified academic staff")</p>
            </a>

            <!-- Exams (Dark) -->
            <a asp-controller="Exams" asp-action="Index" class="bento-card dark-card fade-up delay-3">
                <i class="bi bi-clipboard-check bento-icon"></i>
                <h3 class="bento-title">@(isRtl ? "الامتحانات" : "Exams")</h3>
                <p class="bento-text">@(isRtl ? "جداول وتعليمات الامتحانات" : "Exam schedules and instructions")</p>
            </a>

            <!-- Archives -->
            <a asp-controller="Exams" asp-action="Archive" class="bento-card fade-up delay-4">
                <i class="bi bi-archive bento-icon"></i>
                <h3 class="bento-title">@(isRtl ? "أرشيف الامتحانات" : "Exam Archive")</h3>
                <p class="bento-text">@(isRtl ? "نماذج امتحانات سابقة" : "Previous exam samples")</p>
            </a>

             <!-- Admissions -->
            <a asp-controller="Admissions" asp-action="Index" class="bento-card fade-up delay-4">
                <i class="bi bi-mortarboard bento-icon"></i>
                <h3 class="bento-title">@(isRtl ? "القبول" : "Admissions")</h3>
                <p class="bento-text">@(isRtl ? "شروط وإجراءات القبول" : "Admission requirements and process")</p>
            </a>
        </div>
    </div>
</section>

<!-- ================= ABOUT & VISION ================= -->
<section class="modern-section" style="background-color: #fff;">
    <div class="container">
        <div class="about-split">
            <div class="fade-up">
                <span class="section-label">About Faculty</span>
                <h2 class="section-heading-big" style="margin-bottom: 30px;">
                    @(isRtl ? "عن كلية الهندسة" : "About the Faculty")
                </h2>
                <p class="text-muted lead mb-4">
                    @(isRtl
                    ? "تُعد كلية الهندسة في جامعة مؤتة من الكليات الرائدة على مستوى المملكة، حيث تسعى إلى إعداد مهندسين مؤهلين علميًا وعمليًا."
                    : "The Faculty of Engineering at Mutah University is one of the leading faculties in Jordan.")
                </p>
                
                <div class="vision-cards-grid mt-5">
                     <div class="vision-card-modern">
                        <h5><i class="bi bi-eye text-primary"></i> @(isRtl ? "رؤيتنا" : "Our Vision")</h5>
                         <p class="small text-muted mb-0">
                            @(isRtl
                            ? "الريادة والتميز في التعليم الهندسي والبحث العلمي."
                            : "Leadership and excellence in engineering education and research.")
                        </p>
                     </div>
                     <div class="vision-card-modern">
                        <h5><i class="bi bi-bullseye text-primary"></i> @(isRtl ? "رسالتنا" : "Our Mission")</h5>
                         <p class="small text-muted mb-0">
                            @(isRtl
                            ? "إعداد مهندسين مؤهلين أكاديميًا ومهنيًا."
                            : "Prepare academically and professionally qualified engineers.")
                        </p>
                     </div>
                </div>
            </div>
            
            <div class="about-img-wrapper fade-up delay-2">
                <img src="~/img/logo2.jpg" class="img-fluid" alt="Faculty Building">
            </div>
        </div>
    </div>
</section>

<!-- ================= ALERTS (Preserved Partial) ================= -->
<section class="modern-section bg-light">
    <div class="container">
         <div class="text-center mb-5">
            <span class="section-label">Important Updates</span>
            <h2 class="section-heading-big">@(isRtl ? "الإعلانات والتنبيهات" : "Alerts & Announcements")</h2>
        </div>
        <div class="bg-white p-4 rounded-4 shadow-sm border fade-up">
             <partial name="_AlertsList" model="Model" />
        </div>
    </div>
</section>

<!-- ================= FACULTY & RESEARCH (Dark Mode) ================= -->
<section class="modern-section dark-section">
    <div class="container">
        <div class="text-center mb-5">
             <span class="section-label" style="color:var(--muta-gold);">Distinguished Members</span>
             <h2 class="section-heading-big">@(isRtl ? "أعضاء هيئة التدريس والبحث العلمي" : "Faculty & Research")</h2>
        </div>
        
        <!-- Faculty Carousel/Grid -->
        <div class="row g-4 justify-content-center mb-5">
             <div class="col-md-4 fade-up">
                <div class="faculty-card-modern">
                    <img src="~/img/faculty/mousa.jpg" class="faculty-avatar" alt="">
                    <h5 class="faculty-name">Dr. Ahmad Al-Khaled</h5>
                    <span class="faculty-role">Electrical Engineering</span>
                </div>
            </div>
            <div class="col-md-4 fade-up delay-1">
                <div class="faculty-card-modern">
                    <img src="~/img/faculty/avatar-placeholder.svg" class="faculty-avatar" alt="">
                    <h5 class="faculty-name">Dr. Lina Al-Majali</h5>
                    <span class="faculty-role">Civil Engineering</span>
                </div>
            </div>
             <div class="col-md-4 fade-up delay-2">
                <div class="faculty-card-modern">
                    <img src="~/img/faculty/avatar-placeholder.svg" class="faculty-avatar" alt="">
                    <h5 class="faculty-name">Dr. Omar Al-Rawashdeh</h5>
                    <span class="faculty-role">Mechanical Engineering</span>
                </div>
            </div>
        </div>
        
        <!-- Research Stats -->
        <div class="row text-center mt-5">
             <div class="col-6 col-lg-3 fade-up">
                 <div class="research-stat-circle counter" data-target="45">0</div>
                 <span class="text-light">Research Projects</span>
             </div>
             <div class="col-6 col-lg-3 fade-up delay-1">
                 <div class="research-stat-circle counter" data-target="120">0</div>
                 <span class="text-light">Publications</span>
             </div>
             <div class="col-6 col-lg-3 fade-up delay-2">
                 <div class="research-stat-circle counter" data-target="60">0</div>
                 <span class="text-light">Conferences</span>
             </div>
             <div class="col-6 col-lg-3 fade-up delay-3">
                 <div class="research-stat-circle counter" data-target="18">0</div>
                 <span class="text-light">Partnerships</span>
             </div>
        </div>
    </div>
</section>

<!-- ================= ALUMNI & EMPLOYMENT ================= -->
<section class="modern-section">
    <div class="container">
         <div class="text-center mb-5">
            <span class="section-label">Our Impact</span>
            <h2 class="section-heading-big">Alumni & Outcomes</h2>
        </div>
        
        <div class="bento-grid" style="grid-template-columns: repeat(2, 1fr);">
            <div class="bento-card fade-up">
                <h4 class="mb-3">Industry Leaders</h4>
                <p class="bento-text"> Our alumni work in leading engineering firms, construction companies, and technology organizations locally and internationally.</p>
            </div>
            <div class="bento-card fade-up delay-1">
                 <h4 class="mb-3">Research & Higher Ed</h4>
                <p class="bento-text">Many alumni pursue postgraduate studies and research careers in reputable universities worldwide.</p>
            </div>
        </div>
        
        <!-- Employment Stats Small -->
        <div class="row g-4 mt-5 text-center">
             <div class="col-6 col-md-3">
                 <h2 class="text-primary fw-bold counter" data-target="85">0</h2>
                 <small class="text-muted text-uppercase fw-bold">Employment Rate %</small>
             </div>
              <div class="col-6 col-md-3">
                 <h2 class="text-primary fw-bold counter" data-target="70">0</h2>
                 <small class="text-muted text-uppercase fw-bold">Within 1 Year %</small>
             </div>
             <div class="col-6 col-md-3">
                 <h2 class="text-primary fw-bold counter" data-target="40">0</h2>
                 <small class="text-muted text-uppercase fw-bold">Private Sector %</small>
             </div>
              <div class="col-6 col-md-3">
                 <h2 class="text-primary fw-bold counter" data-target="30">0</h2>
                 <small class="text-muted text-uppercase fw-bold">Public Sector %</small>
             </div>
        </div>
    </div>
</section>

<!-- ================= ACCREDITATION ================= -->
<section class="py-5 text-center bg-white border-top">
    <div class="container">
         <span class="section-label">Quality Assurance</span>
         <h3 class="fw-bold mb-4">Accreditation & Quality</h3>
         <p class="text-muted mx-auto" style="max-width:700px;">
             The Faculty of Engineering is committed to maintaining high academic standards through continuous quality assurance and accredited programs.
         </p>
         <div class="d-flex justify-content-center gap-4 mt-4 opacity-50">
             <!-- Placeholders for partner logos if any (using text for now) -->
             <i class="bi bi-shield-check display-4"></i>
             <i class="bi bi-award display-4"></i>
             <i class="bi bi-patch-check display-4"></i>
         </div>
    </div>
</section>

<!-- ================= SCRIPTS ================= -->
<script>
    // Counter Animation
    document.addEventListener("DOMContentLoaded", () => {
        const counters = document.querySelectorAll(".counter");
        const observer = new IntersectionObserver(entries => {
            entries.forEach(entry => {
                if (entry.isIntersecting && !entry.target.classList.contains("counted")) {
                    const counter = entry.target;
                    counter.classList.add("counted");
                    const target = +counter.dataset.target;
                    let current = 0;
                    const step = Math.max(1, Math.ceil(target / 80)); // Faster
                    const update = () => {
                        current += step;
                        if (current < target) {
                            counter.textContent = current;
                            requestAnimationFrame(update);
                        } else {
                            counter.textContent = target.toLocaleString();
                        }
                    };
                    update();
                }
            });
        }, { threshold: 0.5 });
        counters.forEach(c => observer.observe(c));

        // Fade Up Animation
        const fades = document.querySelectorAll(".fade-up");
        const fadeObs = new IntersectionObserver(entries => {
            entries.forEach(entry => {
                if(entry.isIntersecting) {
                    entry.target.classList.add("show");
                    fadeObs.unobserve(entry.target);
                }
            });
        }, {threshold: 0.1});
        fades.forEach(f => fadeObs.observe(f));
    });
</script>